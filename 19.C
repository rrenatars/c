#define cls clrscr()
#include <stdlib.h>
#include <stdio.h>
#include <conio.h>
#include <string.h>
#include <math.h>
#include <alloc.h>

main()
  {
    int n, x, y;
    cls;
    puts("Введите максимальное число");
    scanf("%d", &n);
    for (x = 2; x <= n; x++);
      {
	for (y = x + 1; y <= n; y++)
	  {
	    if ()
	  }
      }
    getch();
  }
